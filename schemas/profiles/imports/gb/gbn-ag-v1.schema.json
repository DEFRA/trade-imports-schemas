{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "gbn-ag-v1.schema.json",
  "title": "GBN-AG — GB Notification for Animals and Germinals (v1)",
  "description": "Defra import pre-notification schema for live animals and germinals. Derives from the canonical Defra UNVTD core (CertificatePayload)",
  "version": "0.0.1",

  "allOf": [
    {
      "$ref": "../../../core/defra-unvtd-canonical-core-v1.schema.json#/$defs/CertificatePayload"
    },
    {
      "type": "object",
      "required": ["$model", "$type", "exchangedDocument", "specifiedConsignment"],
      "properties": {
        "$model": { "const": "defra/certificate-internal/1" },
        "$type": { "const": "gbn-ag" },

        "exchangedDocument": {
          "type": "object",
          "required": ["identifier", "issuer", "firstSignatoryAuthentication", "referenceDocument"],
          "properties": {
            "identifier": {
              "type": "string",
              "description": "The final agreed format is `GBN-AG-26-7K8M2P`. `GBN` is Great Britain Notification, `AG` is Animals and Germinals, `26` is the two-digit year of allocation, and `7K8M2P` is a six-character Crockford base32 non-guessable body. The scheme is reusable for other GB import journeys (for example `GBN-P-26-XXXXXX` for Products of Animal Origin, `GBN-PP-26-XXXXXX` for Plants)."
            },
            "documentTypeCode": {
              "type": "string",
              "description": "UNTDID 1001 code. GBN-AG leaves this optional and unset today because a UK pre-notification code has not yet been decided. INTRA and CHED carry codes 666/856 and 636 respectively."
            },
            "documentStatusCode": {
              "type": "string",
              "description": "TRACES-boundary value, populated by the gateway on outbound TRACES messages and round-tripped on inbound. UNCL1373 per UN/CEFACT. Internal workflow state is on `notificationStatusCode`."
            },
            "notificationStatusCode": {
              "type": "string",
              "description": "Defra-internal workflow status of this notification. Under GBN-AG the values are `DRAFT`, `SUBMITTED`, `AMEND`, `WITHDRAWN`, drawn from `https://codelists.tbc.defra.gov.uk/gbn-ag-notification-status`."
            },
            "name": {
              "description": "An optional human-readable name for the document. GBN-AG identifies the pre-notification by its `identifier`."
            },
            "traderAssignedId": {
              "description": "Optional reference the trader assigns to the pre-notification - their own internal reference number, carried through unchanged."
            },
            "includedNote": {
              "description": "General notes attached to the document as a whole, distinct from the per-line notes on a trade line's `additionalInformationNote`."
            },
            "firstSignatoryAuthentication": {
              "type": "object",
              "description": "Beyond the primary signature, this hosts the consignment's coded declaration clauses on `includedClause[]`: the reason for import (`identifier` `PURPOSE`) and what the animals are certified for (`identifier` `GOODS_CERTIFIED_AS`), each carrying its value in `content` with the sibling `urlId` naming the `ched_consignment_clause` codelist. GBN-AG requires both clauses to be present.",
              "required": ["includedClause"],
              "properties": {
                "includedClause": {
                  "type": "array",
                  "minItems": 2,
                  "allOf": [
                    {
                      "contains": {
                        "type": "object",
                        "properties": { "identifier": { "const": "PURPOSE" } },
                        "required": ["identifier"]
                      },
                      "description": "Must contain a clause declaring the Reason for Import (identifier='PURPOSE'; urlId points at the ched_consignment_clause codelist)."
                    },
                    {
                      "contains": {
                        "type": "object",
                        "properties": { "identifier": { "const": "GOODS_CERTIFIED_AS" } },
                        "required": ["identifier"]
                      },
                      "description": "Must contain a clause declaring the Animals Certified For category (identifier='GOODS_CERTIFIED_AS'; urlId points at the ched_consignment_clause codelist)."
                    }
                  ]
                }
              },
              "if": {
                "properties": {
                  "includedClause": {
                    "contains": {
                      "type": "object",
                      "properties": {
                        "identifier": { "const": "PURPOSE" },
                        "content": { "const": "INTERNAL_MARKET" }
                      },
                      "required": ["identifier", "content"]
                    }
                  }
                }
              },
              "then": {
                "properties": {
                  "includedClause": {
                    "contains": {
                      "type": "object",
                      "properties": { "identifier": { "const": "INTERNAL_MARKET_PURPOSE" } },
                      "required": ["identifier"]
                    },
                    "description": "When the PURPOSE clause content is INTERNAL_MARKET, an additional INTERNAL_MARKET_PURPOSE clause must be present declaring the fine-grained agricultural intent (slaughter / breeding / fattening / production / transfer of ownership). urlId points at the ched_consignment_clause codelist."
                  }
                }
              }
            },
            "referenceDocument": {
              "type": "array",
              "minItems": 1,
              "contains": {
                "type": "object",
                "properties": {
                  "typeCode": { "const": "853" }
                },
                "required": ["typeCode"]
              },
              "description": "Carries the regulatory documents that accompany the consignment. At least one entry is the veterinary health certificate (`typeCode` `853` on UNTDID 1001); further entries cover the other accompanying documents (transport documents and the like), each distinguished by its `typeCode`. Attachments are referenced by `filename`, `mimeCode`, and `uri` rather than embedded inline."
            }
          }
        },

        "specifiedConsignment": {
          "type": "object",
          "required": [
            "consignorParty",
            "consigneeParty",
            "deliveryParty",
            "importer",
            "originCountry",
            "applicableCrossBorderRegulatoryProcedure",
            "unloadingBaseportLocation",
            "mainCarriageLogisticsTransportMovement",
            "carrier",
            "isOrHasUnweanedAnimals",
            "includedConsignmentItem"
          ],
          "properties": {
            "consignorParty": {
              "description": "The party sending the animals under the trade contract - the exporter in regulatory terms. Distinct from `despatchParty` (the physical place the animals came from); the two may be the same legal entity (a farm that both raises and consigns) but are modelled separately."
            },
            "consigneeParty": {
              "description": "The party receiving the animals under the trade contract. Kept separate from `importer` (the legal importer of record) and `deliveryParty` (the destination party); on a given consignment these may or may not be the same legal entity. Carries `name`, `postalAddress`, and a `definedContact` (telephone, email) for inspection coordination."
            },
            "despatchParty": {
              "description": "The facility the animals were despatched from. In the direct case this is the source farm; in the consolidation case it's the assembly centre or processing plant where the despatch happens. It's a logistics fact (where the despatch event happened), separate from `originCountry`, which is a residency fact (where the animals lived). The two slots are independent: a consignment can carry FR-resident animals despatched via a GB-based assembly centre, with `originCountry: FR` and a GB postal address here. In the direct-origin case they coincide on country. Modelled as a party rather than a location because the regulatory chain pivots on the operator's identity: `identifier` carries the operator's TRACES-issued `operator_activity_id` (with `urlId` naming the codelist), `partyRoleCode` is `\"PW\"` (\"Place of dispatch\") to assert the role on this slot, and `postalAddress` carries the facility's address. One per certificate. Mixed-residence consignments are always split into separate certificates per residence country, regardless of whether they share a despatch facility."
            },
            "deliveryParty": {
              "description": "The party the animals will be delivered to at the immediate destination - the contractual delivery point, which may be temporary. The post-movement permanent home is carried separately: per-animal `permanentLocation`, or the consignment's `finalDestinationLocation` for livestock."
            },
            "carrier": {
              "description": "The person or organisation responsible for moving the goods from the consignment's despatch point to the delivery point. Pre-notifications distinguish commercial from private transporters via the `operator_activity_type` code on `partyTypeCode`; private transporters carry a `private_transporter_approval_number` (issued under the UK welfare-in-transport regime) as the value of `identifier`."
            },
            "originCountry": {
              "$ref": "../../../core/defra-unvtd-canonical-core-v1.schema.json#/$defs/TradeCountry",
              "description": "The country where the animals lived before being exported. For most live-animal imports, country of origin is set by where the animals spent the required residency period defined by the GB health certificate for the species. It's a residency-and-health fact, not a logistics one: how the animals were physically despatched (direct from the farm, via an assembly centre, via a processing plant) doesn't affect what goes here. Required, exactly one per certificate. Consignments with animals from different residence countries are always split across separate certificates, one per residence country - there is no consolidation rule that lets a single certificate carry mixed residence countries. Registered horses re-entering Great Britain are a special case: their country of origin is the country they were last consigned from, not where they originally resided. `exportCountry` is a different fact (the country the consignment was physically despatched from), which usually matches but doesn't have to. Region of origin lives on `subordinateTradeCountrySubDivision` here, because regional disease zoning (FMD-free zones, BTV restriction zones) is a property of where the animals lived."
            },
            "exportCountry": {
              "description": "The country the consignment was exported from. Optional on GBN-AG: populate when it differs from `originCountry` (for example animals produced in one country and exported via another). For the common case where origin and export coincide, leave this empty and carry the value on `originCountry`."
            },
            "transitTradeCountry": {
              "description": "The countries the consignment passes through between origin and destination, one entry per country with `identifier` carrying the ISO 3166-1 alpha-2 code. Material for live animals because each transit country may impose its own welfare checks under long-journey transport rules. Present only when the route crosses other countries."
            },
            "importer": {
              "$ref": "../../../core/defra-unvtd-canonical-core-v1.schema.json#/$defs/TradeParty",
              "description": "The party legally importing the consignment, held as a full party with its own address and contacts. Modelled as a slot distinct from `consigneeParty` because the importer of record and the contractual receiver are frequently different legal entities on UK live-animal imports."
            },
            "finalDestinationLocation": {
              "$ref": "#/$defs/finalDestinationLocation",
              "description": "For livestock the `identifier` inside this slot carries the CPH (County Parish Holding) number of the destination farm, with the sibling `urlId` pinned to `https://codelists.tbc.defra.gov.uk/cph_number`. Pets omit this slot entirely and use per-animal `permanentLocation` instead."
            },
            "applicableCrossBorderRegulatoryProcedure": {
              "type": "array",
              "minItems": 1,
              "items": { "$ref": "#/$defs/crossBorderRegulatoryProcedure" }
            },
            "isOrHasUnweanedAnimals": {
              "type": "boolean",
              "description": "True when the consignment contains any unweaned (non-ablacted) animals. A UK welfare indicator on live-animal import pre-notifications."
            },
            "includedConsignmentItem": {
              "type": "array",
              "minItems": 1,
              "maxItems": 1,
              "description": "GBN-AG consignment item. Exactly one per consignment - a GBN-AG certificate covers a single consignment item, which carries the 'trade lines', one per species or commodity. In UN/CEFACT D23B a `ConsignmentItem` is an item within a consignment separately identified for transport and customs purposes; here it groups the species or commodities carried by the consignment.",
              "items": {
                "type": "object",
                "required": ["includedTradeLineItem"],
                "properties": {
                  "includedTradeLineItem": {
                    "type": "array",
                    "minItems": 1,
                    "items": { "$ref": "#/$defs/gbnAgTradeLineItem" }
                  }
                }
              }
            }
          }
        }
      }
    }
  ],

  "$defs": {
    "finalDestinationLocation": {
      "type": "object",
      "description": "The permanent final destination location for the consignment - UK regulatory livestock tracking. identifier is the County Parish Holding (CPH) number as a bare string. The sibling urlId is pinned to the Defra-curated CPH register.",
      "required": ["identifier", "urlId", "postalAddress"],
      "properties": {
        "identifier": { "type": "string" },
        "urlId": { "type": "string", "format": "uri", "const": "https://codelists.tbc.defra.gov.uk/cph_number" },
        "name": {
          "type": "string"
        },
        "postalAddress": {
          "$ref": "../../../core/defra-unvtd-canonical-core-v1.schema.json#/$defs/TradeAddress"
        }
      }
    },

    "crossBorderRegulatoryProcedure": {
      "type": "object",
      "description": "A cross-border regulatory procedure applied to the consignment - for GBN-AG, the SPS inspection at the UK Border Control Post. typeCode names the procedure; entryCustomsOfficeSpecifiedLogisticsLocation names the BCP. The BCP identifier carries its urlId naming the Defra-curated UK BCP register.",
      "required": ["typeCode", "entryCustomsOfficeSpecifiedLogisticsLocation"],
      "properties": {
        "typeCode": { "type": "string" },
        "urlId": {
          "type": "string",
          "format": "uri",
          "description": "URL to the codelist this procedure typeCode is drawn from (e.g. regulatory_procedure_type)."
        },
        "entryCustomsOfficeSpecifiedLogisticsLocation": {
          "type": "object",
          "required": ["identifier", "urlId"],
          "properties": {
            "identifier": { "type": "string" },
            "urlId": { "type": "string", "format": "uri", "const": "https://codelists.tbc.defra.gov.uk/bcp_reference" },
            "name": {
              "type": "string"
            },
            "postalAddress": {
              "$ref": "../../../core/defra-unvtd-canonical-core-v1.schema.json#/$defs/TradeAddress"
            }
          }
        }
      }
    },

    "gbnAgTradeLineItem": {
      "type": "object",
      "description": "GBN-AG trade line item. One line per species or commodity on the certificate. Each line carries the species or commodity, the line quantity (head count for animals; weight for some germinals), and any notes about that line. Mixed consignments (for example cattle plus sheep, or live animals plus germinals) split across multiple lines, one per species or commodity.",
      "required": ["specifiedTradeProduct", "specifiedLineTradeDelivery"],
      "properties": {
        "specifiedTradeProduct": {
          "type": "array",
          "minItems": 1,
          "items": { "$ref": "#/$defs/gbnAgTradeProduct" }
        },
        "specifiedLineTradeDelivery": {
          "type": "array",
          "minItems": 1,
          "items": { "$ref": "#/$defs/gbnAgLineTradeDelivery" }
        },
        "additionalInformationNote": {
          "type": "array",
          "items": {
            "$ref": "../../../core/defra-unvtd-canonical-core-v1.schema.json#/$defs/IncludedNote"
          },
          "description": "General-purpose annotations on the trade line. GBN-AG carries per-animal identifiers on `individualTradeProductInstance[].identifier[]`; for consumers that follow the TRACES convention, the same identifiers are also emitted here as notes with `subjectCode` `INDIVIDUAL_IDENTIFICATION_NUMBER`."
        }
      }
    },

    "gbnAgTradeProduct": {
      "type": "object",
      "description": "GBN-AG trade product: one species or commodity. CN classification, scientific and common names (resolved from the CN code via Defra reference data), and per-animal identifiers Path B. Country of origin is not held here on GBN-AG - it sits at the consignment level on `specifiedConsignment.originCountry`, since an animal pre-notification carries a single country of origin for the whole consignment.",
      "required": [
        "designatedProductClassification",
        "description",
        "scientificName",
        "commonName"
      ],
      "properties": {
        "description": {
          "type": "string",
          "description": "The free-text description of the commodity. Held at product scope because it describes the commodity itself, not the line that carries delivery quantity - which matters for mixed consignments (for example cats and dogs) where each species sits on its own product. Paired with `scientificName` and `commonName` for full species identification."
        },
        "designatedProductClassification": {
          "type": "array",
          "minItems": 1,
          "items": { "$ref": "#/$defs/gbnAgProductClassification" }
        },
        "typeCode": {
          "type": "string",
          "description": "The trade product's type or form - live animal, semen, embryo, or ova. Drawn from a Defra-curated species-type codelist named by the sibling `urlId`."
        },
        "urlId": {
          "type": "string",
          "format": "uri",
          "description": "URL to the codelist this trade product's typeCode is drawn from."
        },
        "scientificName": {
          "type": "array",
          "minItems": 1,
          "items": { "$ref": "../../../core/defra-unvtd-canonical-core-v1.schema.json#/$defs/UneceTextType" },
          "description": "The species name for the commodity, resolved from Defra reference data keyed on the CN code rather than entered by the trader. Required for live animals, where every commodity code maps to a species."
        },
        "commonName": {
          "type": "array",
          "minItems": 1,
          "items": { "$ref": "../../../core/defra-unvtd-canonical-core-v1.schema.json#/$defs/UneceTextType" },
          "description": "The everyday name for the species, paired with `scientificName` to identify the commodity. Like the scientific name, it is resolved from the CN code via reference data rather than trader-entered."
        },
        "individualTradeProductInstance": {
          "type": "array",
          "items": { "$ref": "#/$defs/tradeProductInstance" },
          "description": "Path B per-animal identifiers (BSP-canonical, purpose-built slot): each instance carries an identifier array (microchip / passport / tattoo / ear tag / leg ring as appropriate per the refdata identifier set for the species)."
        }
      }
    },

    "gbnAgProductClassification": {
      "type": "object",
      "description": "A classification entry on a trade product. `systemId` names the coding system the value is drawn from (`CN` for the customs nomenclature, `SPECIES_CLASS` for the taxonomic class, others as registered). `classCode` is the value within that system. A trade product can carry multiple classification entries when more than one system applies.",
      "required": ["systemId", "classCode"],
      "properties": {
        "systemId": { "type": "string" },
        "classCode": {
          "oneOf": [
            { "type": "string" },
            { "type": "integer" }
          ]
        },
        "urlId": {
          "type": "string",
          "format": "uri",
          "description": "URL to the codelist this classification's classCode is drawn from."
        }
      }
    },

    "tradeProductInstance": {
      "type": "object",
      "description": "An individual animal. identifier is an array of regulatory IDs (an animal can carry microchip + passport + tattoo + ear tag + leg ring together; the refdata identifier set for the species determines which apply). permanentLocation is the per-animal permanent address, where the animal will live long-term once import is complete; different animals on the same line can carry different permanent addresses. All properties are optional; the producer populates whichever apply.",
      "properties": {
        "identifier": {
          "type": "array",
          "items": {
            "type": "object",
            "required": ["typeCode", "content"],
            "description": "A per-animal identifier. typeCode names the kind of identifier (EAR_TAG, PASSPORT, MICROCHIP, TATTOO, LEG_RING) and is a bare string drawn from the animal_identifier_types codelist. content is the identifier value itself - it is not drawn from a central registry (an ear-tag is issued by the farm; a microchip number is unique to the chip). The sibling urlId points to the codelist that typeCode is drawn from.",
            "properties": {
              "content":  { "type": "string" },
              "typeCode": { "type": "string" },
              "urlId":    { "type": "string", "format": "uri", "const": "https://codelists.tbc.defra.gov.uk/animal_identifier_types" }
            }
          }
        },
        "permanentLocation": {
          "$ref": "../../../core/defra-unvtd-canonical-core-v1.schema.json#/$defs/LogisticsLocation",
          "description": "Optional per-animal permanent address. The address where this individual animal will permanently reside after import (e.g. for pets imported to a business address and then shipped onwards to their owner's home). Shown for commodities where the producer's refdata indicates a permanent address applies."
        }
      }
    },

    "gbnAgLineTradeDelivery": {
      "type": "object",
      "description": "GBN-AG line delivery: the per-line quantity. The unit varies by commodity per Defra refdata routing — most live animals use H87 (piece count via numberOfAnimals); some commodities (e.g. Sus scrofa domesticus embryos/ova, commodity 203) use KGM (kilograms). The schema accepts any positive number with any string unit code; the source system enforces the refdata-driven unit per commodity.",
      "required": ["productUnitQuantity"],
      "properties": {
        "productUnitQuantity": {
          "type": "object",
          "required": ["content", "unitCode"],
          "properties": {
            "content": { "type": "number", "exclusiveMinimum": 0 },
            "unitCode": { "type": "string" }
          }
        }
      }
    }
  }
}
